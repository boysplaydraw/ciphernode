{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal desktop permissions for CipherNode.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
